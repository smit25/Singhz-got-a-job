---
description: Build/store master resume PDF (default upload)
---

Build and store your default resume PDF using singhz-got-a-job resume mode.

Load the singhz-got-a-job skill:
```
skill({ name: "singhz-got-a-job" })
```

Then run with mode `resume`.
